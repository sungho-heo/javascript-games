doctype html 
html(lang="ko")
    head
        link(rel="stylesheet" href="https://unpkg.com/mvp.css@1.12/mvp.css")
        title #{pageTitle} - hotube
    body 
        header
            nav
                ul
                    if internUser.loginOn
                        li
                            a(href="/logout") logout
                    else
                        li
                            a(href="/login") login 
            h1=pageTitle
        main
            block content
    include partials/footer.pug